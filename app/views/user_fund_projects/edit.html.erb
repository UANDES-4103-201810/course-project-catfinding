<h1>Confirming Fund</h1>

<%= form_for(@user_fund_project) do |form| %>
  <% if  @user_fund_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize( @user_fund_project.errors.count, "error") %> prohibited this  @user_fund_project from being saved:</h2>

      <ul>
        <%  @user_fund_project.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <h4 >
    Hey <%= @user.name %>! You are confirming your fund to <%= @project.title %>
  </h4>
  <br><br>
  <div class="actions">

    <td><%= link_to "Confirm!", "/user_fund_project_confirm/"+ @user_fund_project.id.to_s + "?confirmed=true", class: "btn btn-primary btn-lg", method: :post %></td>
  </div>
<% end %>
<br><%= link_to 'Home', root_path   , { :class=>"btn btn-primary" }%>