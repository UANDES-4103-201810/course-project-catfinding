<h1>Editing Wishlist Project</h1>

<%= render 'form', wishlist_project: @wishlist_project %>

<%= link_to 'Show', @wishlist_project   , { :class=>"btn btn-primary" }%>
<%= link_to 'Back', wishlist_projects_path   , { :class=>"btn btn-primary" }%>
