<h1>Editing Wishlist</h1>

<%= render 'form', wishlist: @wishlist %>

<%= link_to 'Show', @wishlist   , { :class=>"btn btn-primary" }%>
<%= link_to 'Back', wishlists_path   , { :class=>"btn btn-primary" }%>
