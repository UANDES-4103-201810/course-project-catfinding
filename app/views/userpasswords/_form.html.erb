<%= form_with(model: userpassword, local: true) do |form| %>
  <% if userpassword.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(userpassword.errors.count, "error") %> prohibited this userpassword from being saved:</h2>

      <ul>
      <% userpassword.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
